# Shared runtime dependencies for laptop development and Raspberry Pi.
# On Linux (including Raspberry Pi), PyQt5 is provided by apt (python3-pyqt5)
# and exposed to the venv via --system-site-packages. QtWebEngine bindings
# (in-process web browsing for streaming tiles) similarly come from apt on
# Linux (python3-pyqt5.qtwebengine) - install-bro/update must provision that
# package; see broSmartTV/py/interface/web_interface.py.

PyQt5==5.15.11; platform_system == "Windows"
PyQtWebEngine==5.15.7; platform_system == "Windows"
qtinter==0.11.0
bleak==0.22.3
aiohttp==3.14.1
aiortc==1.14.0
sounddevice==0.5.2
